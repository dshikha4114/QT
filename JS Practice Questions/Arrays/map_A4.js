//4. Multiply all elements using map() 
    //Question: Multiply all elements by 3. 
   //Input: 
const numbers = [1, 2, 3, 4, 5]; 

const Mul=numbers.map(number => number*3);
console.log(Mul);
  //Expected Output: 
//[3, 6, 9, 12, 15] 